*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build — intentionally COMMITTED. Assets are built locally/in CI and
# checked in so the 1GB production droplet never has to run Vite (it OOMs). The
# server just `git pull`s the compiled output; deploy.sh does NOT build.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
.claude/
